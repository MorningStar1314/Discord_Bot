ctx = context(上下文)
A:嗨(上文)  (使用者,id,所在伺服器,所在頻道)
B:安安(下文)


Cog
將函數指令分成數類，需要時再Load即可，不須時也可Unload

*參數==>指標
        找到該參數的記憶體位置 將所有為參數名稱的資料放入該位置
        使該參數可以儲存不同資料 

        特點:可變長度的參數    [資料有多少 參數數量就有多少]

     
__init__ 初始化設定